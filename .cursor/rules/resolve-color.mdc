---
description: Color / Grade in the DaVinci Resolve MCP — grading, correcting, shot matching, developing looks, or applying/modifying LUTs, CDLs, DRX grades, or copied grades
alwaysApply: false
---

<!-- GENERATED by scripts/agent-rules/generate.mjs — edit that, not this file. -->

Resolve Color / Grade work. Apply when: grading, correcting, shot matching, developing looks, or applying/modifying LUTs, CDLs, DRX grades, or copied grades.

Two servers — compute offline, apply live:
- Live (running Resolve, `davinci-resolve`): timeline_item_color (grade_boundary_report / safe_set_cdl / safe_apply_drx / grade_version_*), graph, gallery_stills, color_group
- Offline (.drp/.drt/.drx + DB, no Resolve, `davinci-resolve-advanced`): drx (match_to_reference, skin_match, shot_match, grade_transfer, cdl_io, lut_apply, relayout, scope_read, verify_grade, …), project_db
- Key rule: Frame-first — inspect Resolve-rendered frames before applying any grade/look/LUT/CDL/DRX. Compute offline, apply live. drx generate/merge default to space="ui"; a warnings array means a hue curve rendered FLAT. safe_apply_drx defaults to V1/item0 — pass explicit track/item indices and back up a still first.

Depth: docs/kernels/color-grade-kernel.md + docs/guides/color-decision-guide.md. On-demand in any MCP client: the `/color_grade_workflow` slash prompt. Claude Code skill: `resolve-color`. Never modify/transcode/derive source media (AGENTS.md).
